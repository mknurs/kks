{% extends "includes/base.twig" %}

{% block content %}
<main role="main" id="main" class="list">
  {% for page in pages(current_page.id, depth=null)|sort_by(["modificationTime"])|reverse if page.meta.template == "post" and not page.hidden %}
    {{ include("includes/card.twig") }}
  {% endfor %}
</main>
{% endblock %}
